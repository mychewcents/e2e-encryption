/**
 * Takes in the algorithm to use and the text to encrypt
 * @param {String} algo Algorithm type
 * @param {String} text Plain text
 */

const Encrypt = (algo, text) => text;

export default Encrypt;
